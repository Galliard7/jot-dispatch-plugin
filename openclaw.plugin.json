{
  "id": "jot-dispatch",
  "name": "Jot Dispatch",
  "description": "Deterministic dispatch for /jot, /anchors, and /idea slash commands — bypasses the LLM.",
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
